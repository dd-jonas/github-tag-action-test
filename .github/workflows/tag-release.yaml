name: Tag release
on:
  pull_request:
    branches:
      - production
    types:
      - closed
jobs:
  tag-release:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo PR successfully merged
